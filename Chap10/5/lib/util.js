export class IntegerLimits{
    static main(){
        console.log('整数型の表現範囲');
        console.log(`Number：${Number.MIN_VALUE}〜${Number.MAX_VALUE}`);
        console.log(`Number(INTEGER)：${Number.MIN_SAFE_INTEGER}〜${Number.MAX_SAFE_INTEGER}`);
    }
}